## Future Enhancements I'd Implement

1. **Multi-tenant support**: Different rules/thresholds per API key
2. **Machine learning component**: Unsupervised anomaly detection on request patterns
3. **Integration with Slack**: Real-time alerts when block threshold crossed
4. **Rule versioning**: Track when rules change, correlate with detection accuracy

These would be the next steps if this became a real product.